# Testing dependencies
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
httpx==0.25.2  # For testing FastAPI endpoints
faker==22.0.0  # For generating test data
aiosqlite>=0.19.0  # Async SQLite driver for unit tests (no PostgreSQL needed)